if [ $1 ]
then
  TARGET=$1
else
  TARGET="gaeupload"
fi

export PYTHONPATH="$PYTHONPATH:$TARGET:."
nosetests -v -w tests --gae-lib-root ~/google_appengine --gae-application $TARGET --with-gae --debug-log debug.log

