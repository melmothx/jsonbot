if [ -e "~/google_appengine" ]
then
    echo "i need google_appengine installed in ~"
else
    export PATH=".:./bin:$PATH"
    jsb-bootstrap
    jsb-init
    cp releases/$1/config/app.yaml releases/$1
fi