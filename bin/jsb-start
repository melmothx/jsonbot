if [ $1 ]
then
    TARGET=$1
else
    TARGET="jsonbot"
fi

if [ -e "~/google_appengine" ]
then
    echo "i need google_appengine installed in ~"
else
  echo "target is $TARGET"
  export PATH=".:./bin:$PATH"
  jsb-bootstrap $TARGET
fi
